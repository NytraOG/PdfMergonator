<Window x:Class="Main.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

        mc:Ignorable="d"
        Title="MainView" Height="461" Width="800">

    <Grid >
        <Button Content="Import" Command="{Binding ImportFilesCommand}" HorizontalAlignment="Left" Margin="10,381,0,0" VerticalAlignment="Top" Width="100" Height="30"/>
        <TextBox HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" Text="{Binding PdfTextContent}" VerticalAlignment="Top" Width="780" Height="366"/>
        <Button Content="Clear" Command="{Binding ClearCommand}" HorizontalAlignment="Left" Margin="115,381,0,0" VerticalAlignment="Top" Width="100" Height="30"/>
    </Grid>
</Window>